DROP DATABASE IF EXISTS db_api_profile;
CREATE DATABASE db_api_profile CHARSET utf8mb4;
USE db_api_profile;

CREATE TABLE profile (
    id INT(11) NOT NULL AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    apellido VARCHAR(100) NOT NULL,
    correo VARCHAR(100) NOT NULL,
    password VARCHAR(100) NOT NULL,
    celular VARCHAR(100) NOT NULL,
    description VARCHAR(100) NOT NULL,
    imagen VARCHAR(100) NOT NULL,
    PRIMARY KEY (id)
);

INSERT INTO profile(name, apellido, correo, password, celular, description, imagen) VALUES('Jake', 'Perez', 'j.perez@gmail.com', '945454156', 'Amable', 'google.com');
commit;
